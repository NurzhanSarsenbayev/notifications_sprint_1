# Notification Service  
Проектное задание 10-го спринта (Yandex Praktikum)

Сервис нотификаций — это централизованный компонент онлайн-кинотеатра,
который принимает события от других сервисов, формирует персонализированные
уведомления и доставляет их пользователям через различные каналы (email, push, websocket).

Архитектура сервиса построена по паттерну **API → очередь → воркеры**.

---

## 📦 Компоненты сервиса

- **Notification API (FastAPI)**  
  Принимает события (`Event`) от Auth, Content, UGC, админки.  
  Решает, кому и что отправлять.  
  Формирует задания (`NotificationJob`) и публикует их в Kafka.

- **Kafka**  
  Брокер сообщений.  
  Используются топики:
  - `notifications.outbox` — основная очередь заданий;
  - `notifications.retry` — повторные попытки;
  - `notifications.dlq` — невозможные к обработке сообщения.

- **Notification Worker(s)**  
  Читает задания из Kafka, получает данные пользователя,
  рендерит шаблоны сообщений и отправляет их выбранным каналом.

---

## 📚 Документация

- [ARCHITECTURE.md](docs/ARCHITECTURE.md) — подробная архитектура сервиса  
- [EVENTS.md](docs/EVENTS.md) — контракты внешних событий (`Event`)  
- [QUEUE_JOBS.md](docs/QUEUE_JOBS.md) — контракты сообщений в Kafka (`NotificationJob`)  
- [API.md](docs/API.md) — спецификация Notification API

---

## 🚀 Запуск проекта (появится позже)

Здесь будет описано:
- запуск через docker-compose,
- переменные окружения,
- команды для API и worker.

---

## 🛠 Используемые технологии

- Python 3.11+
- FastAPI
- Kafka
- Docker / docker-compose

---

## 📂 Структура проекта

```
src/
  notifications/
    api/        # HTTP API для приёма событий и создания NotificationJob
    worker/     # Воркер(ы), читающие сообщения из Kafka
    common/     # Общие настройки и обёртки для Kafka
docs/           # Архитектура и контракты (Event, NotificationJob)
infra/          # docker-compose и инфраструктурные файлы

```

---
