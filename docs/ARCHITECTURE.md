# Архитектура сервиса нотификаций

Сервис нотификаций обеспечивает доставку сообщений пользователям по событиям,
которые генерируются различными сервисами онлайн-кинотеатра.
Система построена вокруг очереди сообщений Kafka и набора воркеров, которые
выполняют персонализацию и фактическую отправку уведомлений.

---

## 🧩 Компоненты

### 1. Notification API (FastAPI)
Центральный вход в систему нотификаций.

Задачи:
- приём событий от Auth, Content, UGC, админки;
- определение, кому нужно отправить уведомление;
- формирование `NotificationJob`;
- публикация заданий в Kafka (`notifications.outbox`);
- логика отложенных/плановых сообщений (если потребуется).

API **не отправляет** сообщения самостоятельно.

---

### 2. Kafka

Используется как надёжный брокер сообщений.  
Топики:

| Топик | Назначение |
|-------|-------------|
| `notifications.outbox` | Основная очередь задач `NotificationJob` |
| `notifications.retry` | Повторные попытки (backoff-логика) |
| `notifications.dlq` | Dead Letter Queue: сообщения, которые невозможно обработать |

Все сообщения имеют единый формат (`NotificationJob`).

---

### 3. Notification Worker(s)

Независимые процессы, которые обрабатывают очередь.

Функции:
1. Чтение `NotificationJob` из Kafka.
2. Запрос данных пользователя в Auth по `user_id`.
3. Рендер шаблона (`template_code`, `locale`).
4. Отправка через выбранный канал:
   - email (SMTP / внешний провайдер / лог)
   - push (stub или реальный пуш сервис)
   - websocket (через WS-gateway)
5. Логирование результата.
6. Retry c backoff или отправка в `notifications.dlq`.

Worker должен быть **идемпотентен** (по `job_id`).

---

## 🔄 Поток данных

Пример: регистрация пользователя.

[Auth Service]
|
| 1. POST /events (user_registered)
|
v
[Notification API]
|
| 2. Формирует NotificationJob
| Публикует в Kafka
|
v
[KAFKA: notifications.outbox]
|
| 3. Worker потребляет сообщение
v
[Notification Worker]
|
| 4. Получает профиль пользователя
| 5. Подставляет данные в шаблон
| 6. Отправляет email
v
[Email provider / SMTP]


---

## 🗂 Схемы данных

Детальная спецификация:

- формата входных событий см. в [EVENTS.md](EVENTS.md)  
- формата сообщений в очередь см. в [QUEUE_JOBS.md](QUEUE_JOBS.md)

---

## ✔ Ключевые требования к системе

- надёжность: retry + dlq;
- идемпотентность отправки;
- ограничение нагрузки на Auth (ограничение воркеров и частота запросов);
- персонализация сообщений (данные пользователя подгружаются в момент обработки);
- расширяемость (новые шаблоны и события добавляются без изменения воркеров);
- возможность отложенных и плановых рассылок (send_after / campaign scheduler).